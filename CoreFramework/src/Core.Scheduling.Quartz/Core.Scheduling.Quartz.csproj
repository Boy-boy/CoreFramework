<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>$(TargetFramework)</TargetFramework>
		<ImplicitUsings>$(ImplicitUsings)</ImplicitUsings>
		<Nullable>$(Nullable)</Nullable>
		<LangVersion>$(LangVersion)</LangVersion>
		<Version>$(Version)</Version>
		<Authors>$(Authors)</Authors>
		<Description>Core.Scheduling 的 Quartz.NET 适配器：把同一份 IScheduledHandler 业务代码以集群模式跑起来，由 Quartz AdoJobStore + QRTZ_LOCKS 仲裁同一时刻只一个节点触发。</Description>
		<PackageProjectUrl>$(PackageProjectUrl)</PackageProjectUrl>
		<PackageLicenseExpression>$(PackageLicenseExpression)</PackageLicenseExpression>
		<PackageTags>scheduling;quartz;cluster;hosted-service;cron</PackageTags>
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="Quartz" />
		<PackageReference Include="Quartz.Extensions.Hosting" />
		<PackageReference Include="Quartz.Serialization.SystemTextJson" />
		<PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
		<PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
		<PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
		<PackageReference Include="Microsoft.Extensions.Options" />
		<PackageReference Include="Microsoft.Extensions.Options.ConfigurationExtensions" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\Core.Scheduling.Abstractions\Core.Scheduling.Abstractions.csproj" />
		<ProjectReference Include="..\Core.Scheduling\Core.Scheduling.csproj" />
		<ProjectReference Include="..\Core.Modularity\Core.Modularity.csproj" />
	</ItemGroup>

	<ItemGroup>
		<None Include="sql\**\*.sql" Pack="true" PackagePath="sql\%(RecursiveDir)%(Filename)%(Extension)" />
	</ItemGroup>

</Project>
